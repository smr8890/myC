#include "stdio.h"

int main(){
    int T;
    scanf("%d",&T);
    while (T--){
        int n;
        scanf("%d",&n);
        int a[10005];
        for (int i = 0; i < n; ++i) {
            scanf("%d",&a[i]);
        }
    }
    return 0;
}